# Dependencies
node_modules/
pnpm-lock.yaml
# (package-lock.json / yarn.lock は通常コミットするので ignore しない)

# Build / dist artifacts
dist/
dist-ssr/
dist_test/
dist_final_check/
test-results/
.tmp/
tmp/
build_log*.txt
build_output.txt
all_ts_files.txt
last_part_index.txt
last_part_index.txt

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
debug_*.txt

# Env / local config
*.local
.env.local
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Nested workspace folder (avoid separate repo/worktree)
FlashCard-Master/

# Firebase
.firebase/
serviceAccountKey.json
secrets/

# Functions build output
functions/lib/
functions/.env*

# Service worker
# NOTE: keep this ignored ONLY if it is generated by build.
public/sw.js

# Local docs / reports
MANIFEST_FIX_SUMMARY.md
# Test artifacts
test-results/

