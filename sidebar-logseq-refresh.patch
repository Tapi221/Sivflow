--- a/src/layout/Sidebar.tsx
+++ b/src/layout/Sidebar.tsx
@@ -11,6 +11,11 @@
   icon: React.ReactNode;
 };
 
+type NavSection = {
+  title: string;
+  items: NavItem[];
+};
+
 const FolderIcon = () => (
   <svg
     width="16"
@@ -262,15 +267,23 @@
   </svg>
 );
 
-const NAV_ITEMS: NavItem[] = [
+const PRIMARY_NAV_ITEMS: NavItem[] = [
   { to: "/folders", label: "フォルダ", icon: <FolderIcon /> },
-  { to: "/tag-map", label: "タグマップ", icon: <TagMapIcon /> },
   { to: "/dictionary", label: "辞書", icon: <DictionaryIcon /> },
   { to: "/questions", label: "疑問集", icon: <QuestionIcon /> },
   { to: "/calendar", label: "カレンダー", icon: <CalendarIcon /> },
+];
+
+const SECONDARY_NAV_ITEMS: NavItem[] = [
+  { to: "/tag-map", label: "タグマップ", icon: <TagMapIcon /> },
   { to: "/gallery", label: "ギャラリー", icon: <GalleryIcon /> },
+  { to: "/directory", label: "ディレクトリ", icon: <DirectoryIcon /> },
   { to: "/trash", label: "ゴミ箱", icon: <TrashIcon /> },
-  { to: "/directory", label: "ディレクトリ", icon: <DirectoryIcon /> },
+];
+
+const NAV_SECTIONS: NavSection[] = [
+  { title: "メイン", items: PRIMARY_NAV_ITEMS },
+  { title: "整理", items: SECONDARY_NAV_ITEMS },
 ];
 
 export const Sidebar = () => {
@@ -287,24 +300,53 @@
     navigate({ search: `?${next.toString()}` });
   };
 
+  const renderNavLink = ({ to, label, icon }: NavItem) => {
+    return (
+      <NavLink
+        key={to}
+        to={to}
+        className={({ isActive }) =>
+          getSidebarNavItemClassName({
+            isActive:
+              to === "/folders" ? isActive && !isHomeOnlyMode : isActive,
+          })
+        }
+      >
+        <SidebarNavIcon>{icon}</SidebarNavIcon>
+        <span className="sidebar__nav-label">{label}</span>
+      </NavLink>
+    );
+  };
+
   return (
     <aside className="sidebar">
+      <div className="sidebar__header">
+        <NavLink
+          to="/folders"
+          className="sidebar__workspace-link"
+          aria-label="ホームに移動"
+        >
+          <span className="sidebar__workspace-icon" aria-hidden="true">
+            <FolderIcon />
+          </span>
+          <span className="sidebar__workspace-copy">
+            <span className="sidebar__workspace-name">FlashCard Master</span>
+            <span className="sidebar__workspace-meta">メインメニュー</span>
+          </span>
+        </NavLink>
+      </div>
+
       <nav className="sidebar__nav" aria-label="メインナビゲーション">
-        {NAV_ITEMS.map(({ to, label, icon }) => (
-          <NavLink
-            key={to}
-            to={to}
-            className={({ isActive }) =>
-              getSidebarNavItemClassName({
-                isActive:
-                  to === "/folders" ? isActive && !isHomeOnlyMode : isActive,
-              })
-            }
-          >
-            <SidebarNavIcon>{icon}</SidebarNavIcon>
-            <span className="sidebar__nav-label">{label}</span>
-          </NavLink>
-        ))}
+        {NAV_SECTIONS.map(({ title, items }) => {
+          return (
+            <div key={title} className="sidebar__section">
+              <div className="sidebar__section-label">{title}</div>
+              <div className="sidebar__section-items">
+                {items.map(renderNavLink)}
+              </div>
+            </div>
+          );
+        })}
       </nav>
 
       <div className="sidebar__footer">

--- a/src/layout/Sidebar.css
+++ b/src/layout/Sidebar.css
@@ -1,46 +1,134 @@
 .sidebar {
-  --sidebar-bg: #f6f1eb;
-  --sidebar-border: rgba(218, 209, 199, 0.92);
-  --sidebar-text: #403731;
-  --sidebar-text-muted: #73685e;
-  --sidebar-hover-bg: rgba(241, 232, 222, 0.92);
-  --sidebar-active-bg: rgba(236, 226, 215, 0.98);
-  --sidebar-icon: #7b6f64;
-  --sidebar-icon-active: #544940;
-  --sidebar-footer-border: rgba(218, 209, 199, 0.78);
-
-  display: flex;
-  flex-direction: column;
-  width: 220px;
+  --sidebar-bg: #f3f4f6;
+  --sidebar-border: rgba(15, 23, 42, 0.08);
+  --sidebar-text: #111827;
+  --sidebar-text-muted: #6b7280;
+  --sidebar-text-soft: #9ca3af;
+  --sidebar-hover-bg: rgba(226, 232, 240, 0.72);
+  --sidebar-active-bg: #e5e7eb;
+  --sidebar-icon: #6b7280;
+  --sidebar-icon-active: #111827;
+  --sidebar-footer-border: rgba(15, 23, 42, 0.08);
+  --sidebar-focus-ring: rgba(100, 116, 139, 0.28);
+
+  display: flex;
+  flex-direction: column;
+  width: 100%;
   height: 100%;
-  background: var(--sidebar-bg, #f6f1eb);
-  color: var(--sidebar-text, #403731);
-  border-right: 1px solid var(--sidebar-border, rgba(218, 209, 199, 0.92));
+  min-height: 0;
+  background: var(--sidebar-bg);
+  color: var(--sidebar-text);
+  border-right: 1px solid var(--sidebar-border);
   overflow-y: auto;
-  padding: 12px 12px;
+  padding: 14px 12px 12px;
   box-sizing: border-box;
 }
 
-.sidebar__nav {
-  display: flex;
-  flex-direction: column;
-  gap: 1px;
-  flex: 1;
-}
-
-.sidebar__nav-item {
-  display: flex;
-  align-items: center;
-  gap: 8px;
-  min-height: 36px;
-  padding: 0 10px;
-  border-radius: 7px;
-  font-size: 14px;
-  color: var(--sidebar-text-muted, #73685e);
+.sidebar__header {
+  margin-bottom: 14px;
+}
+
+.sidebar__workspace-link {
+  display: flex;
+  align-items: center;
+  gap: 12px;
+  min-height: 48px;
+  padding: 0 12px;
+  border-radius: 12px;
+  color: var(--sidebar-text);
   text-decoration: none;
   transition:
     background-color 0.15s ease,
-    color 0.15s ease;
+    color 0.15s ease,
+    box-shadow 0.15s ease;
+}
+
+.sidebar__workspace-link:hover {
+  background: rgba(226, 232, 240, 0.52);
+}
+
+.sidebar__workspace-link:focus-visible,
+.sidebar__nav-item:focus-visible,
+.sidebar__settings-btn:focus-visible {
+  outline: none;
+  box-shadow: inset 0 0 0 1px var(--sidebar-focus-ring);
+}
+
+.sidebar__workspace-icon {
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  width: 18px;
+  min-width: 18px;
+  height: 18px;
+  color: var(--sidebar-icon);
+}
+
+.sidebar__workspace-copy {
+  display: flex;
+  min-width: 0;
+  flex-direction: column;
+  gap: 1px;
+}
+
+.sidebar__workspace-name {
+  font-size: 15px;
+  font-weight: 600;
+  line-height: 1.2;
+  color: var(--sidebar-text);
+}
+
+.sidebar__workspace-meta {
+  font-size: 11px;
+  line-height: 1.3;
+  color: var(--sidebar-text-muted);
+}
+
+.sidebar__nav {
+  display: flex;
+  flex: 1;
+  min-height: 0;
+  flex-direction: column;
+  gap: 14px;
+}
+
+.sidebar__section {
+  display: flex;
+  flex-direction: column;
+  gap: 6px;
+}
+
+.sidebar__section-label {
+  padding: 0 10px;
+  font-size: 11px;
+  font-weight: 700;
+  line-height: 1.2;
+  letter-spacing: 0.08em;
+  color: var(--sidebar-text-soft);
+  text-transform: uppercase;
+}
+
+.sidebar__section-items {
+  display: flex;
+  flex-direction: column;
+  gap: 2px;
+}
+
+.sidebar__nav-item {
+  display: flex;
+  align-items: center;
+  gap: 10px;
+  min-height: 40px;
+  padding: 0 10px;
+  border-radius: 10px;
+  font-size: 14px;
+  font-weight: 500;
+  color: var(--sidebar-text-muted);
+  text-decoration: none;
+  transition:
+    background-color 0.15s ease,
+    color 0.15s ease,
+    box-shadow 0.15s ease;
   width: 100%;
   box-sizing: border-box;
   border: none;
@@ -48,17 +136,18 @@
 }
 
 .sidebar__nav-item:hover {
-  background: var(--sidebar-hover-bg, rgba(241, 232, 222, 0.92));
-  color: var(--sidebar-text, #403731);
+  background: var(--sidebar-hover-bg);
+  color: var(--sidebar-text);
 }
 
 .sidebar__nav-item--active {
-  background: var(--sidebar-active-bg, rgba(236, 226, 215, 0.98));
-  color: var(--sidebar-text, #403731);
+  background: var(--sidebar-active-bg);
+  color: var(--sidebar-text);
   font-weight: 600;
 }
 
 .sidebar__settings-btn {
+  width: 100%;
   background: none;
   border: none;
   cursor: pointer;
@@ -73,26 +162,28 @@
   min-width: 18px;
   height: 18px;
   flex-shrink: 0;
-  color: var(--sidebar-icon, #7b6f64);
+  color: var(--sidebar-icon);
   opacity: 1;
   transition: color 0.15s ease;
 }
 
 .sidebar__nav-label {
+  min-width: 0;
   font-size: 14px;
   line-height: 1.2;
+  letter-spacing: 0.01em;
 }
 
 .sidebar__nav-item--active .sidebar__nav-icon,
 .sidebar__nav-item:hover .sidebar__nav-icon {
-  color: var(--sidebar-icon-active, #544940);
+  color: var(--sidebar-icon-active);
   opacity: 1;
 }
 
 .sidebar__footer {
   margin-top: auto;
-  padding-top: 8px;
-  border-top: 1px solid var(--sidebar-footer-border, rgba(218, 209, 199, 0.78));
+  padding-top: 10px;
+  border-top: 1px solid var(--sidebar-footer-border);
   display: flex;
   flex-direction: column;
   gap: 8px;
@@ -101,18 +192,18 @@
 .sidebar__sync-status {
   display: flex;
   align-items: center;
-  gap: 6px;
+  gap: 8px;
   min-height: 18px;
   padding: 0 10px 2px;
-  color: var(--sidebar-text-muted, #73685e);
+  color: var(--sidebar-text-muted);
   font-size: 11px;
-  line-height: 1.2;
+  line-height: 1.3;
 }
 
 .sidebar__sync-status-dot {
-  width: 6px;
-  min-width: 6px;
-  height: 6px;
+  width: 7px;
+  min-width: 7px;
+  height: 7px;
   border-radius: 9999px;
   background: currentColor;
   opacity: 0.9;
@@ -126,7 +217,7 @@
 }
 
 .sidebar__sync-status--success {
-  color: #7f945e;
+  color: #7b8f58;
 }
 
 .sidebar__sync-status--syncing {
@@ -139,5 +230,5 @@
 
 .sidebar__sync-status--waiting,
 .sidebar__sync-status--idle {
-  color: var(--sidebar-text-muted, #73685e);
-}
+  color: var(--sidebar-text-muted);
+}

--- a/src/layout/AppLayout.css
+++ b/src/layout/AppLayout.css
@@ -1,6 +1,8 @@
 .app-layout {
+  --app-shell-sidebar-width: 264px;
+
   display: grid;
-  grid-template-columns: 220px minmax(0, 1fr);
+  grid-template-columns: var(--app-shell-sidebar-width) minmax(0, 1fr);
   grid-template-rows: 1fr;
   height: 100%;
   flex: 1;

--- a/src/layout/sidebarNavItem.utils.ts
+++ b/src/layout/sidebarNavItem.utils.ts
@@ -10,8 +10,8 @@
   className,
 }: SidebarNavItemClassNameOptions = {}) => {
   return cn(
-    "sidebar__nav-item",
-    isActive && "sidebar__nav-item--active",
+    "sidebar__nav-item ds-nav-action",
+    isActive && "sidebar__nav-item--active ds-nav-action--active",
     className,
   );
 };
