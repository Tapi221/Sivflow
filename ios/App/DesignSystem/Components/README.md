# iOS Design System Components

- Placeholder for future SwiftUI component implementations.
- Shared design contract comes from `design/tokens` and `design/components`.
- Keep platform navigation chrome and window behavior outside this folder.
