export const CARD_BASE_WIDTH = 480;
export const CARD_ROW_PX = 24;
// Top safe area reserved for card corner actions.
// Keep this shared across editor/view so spacing is consistent everywhere.
export const CARD_TOP_PADDING_PX = 40;
