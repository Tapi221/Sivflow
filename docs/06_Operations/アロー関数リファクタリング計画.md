# 実装計画: アロー関数へのリファクタリング

配列メソッドのコールバックや `setInterval`、Promise 等に渡される関数で、`this` を DOM 要素として利用しないものをアロー関数に書き換え、コードの記述スタイルを統一します。

## 概要
現状の調査により特定された以下の箇所をアロー関数に書き換えます。また、実施時に漏れがないか再スキャンを行います。

## 変更内容

### 1. ユーザーインターフェース・ページコンポーネント

#### [MODIFY] [StudyMode.jsx](file:///c:/FlashcardMaster/src/Pages/StudyMode.jsx)
- `setInterval` に渡されている `function()` をアロー関数 `() =>` に書き換えます。

#### [MODIFY] [toaster.tsx](file:///c:/FlashcardMaster/src/Components/ui/toaster.tsx)
- `toasts.map` に渡されている `function` をアロー関数に書き換えます。

## 検証計画

### 自動テスト
- `npm run build` を実行し、構文エラーが発生しないことを確認します。

### 手動確認
- 学習モード（StudyMode）を完了し、紙吹雪（confetti）が表示されることを確認します（`setInterval` の動作確認）。
- トースト通知が表示される際にエラーが発生しないことを確認します。
