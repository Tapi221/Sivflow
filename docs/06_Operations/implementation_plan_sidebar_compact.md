# 実装計画: サイドバーのさらなるコンパクト化と上部表示最適化

## ゴール
1. サイドバーの「右側の余白（＝サイドバーの幅）」を縮小し、作業領域を広げる。
2. サイドバー内およびメインコンテンツの表示開始位置を「限界まで上」に上げ、トップの余白を最小化する。

## Proposed Changes

### レイアウト調整
#### [MODIFY] [Layout.tsx](file:///c:/FlashcardMaster/src/Layout.tsx)
- サイドバー幅 (`w-24` = 96px) を `w-[72px]` (72px) に縮小。
  - 合わせて `main` の左マージン `md:ml-24` を `md:ml-[72px]` に変更。
- サイドバー開閉ボタン (`Menu`) のサイズと位置をさらにコンパクトに変更。
  - `w-10 h-10` → `w-9 h-9`
  - `top-1.5` → `top-1`
  - `left-2` → `left-1.5`
- サイドバー内の上部スペーサー (`h-14`) を `h-10` (40px) に短縮。
- 同期インジケーター位置も `top-1.5` → `top-1`、`right-4` → `right-2` に調整。

### フォルダツリー調整
#### [MODIFY] [FolderTree.tsx](file:///c:/FlashcardMaster/src/Components/folder/FolderTree.tsx)
- コンテナの `py-2` (上下パディング) を削除または縮小し、ツリーがより上から始まるようにする。

## Verification Plan

### Manual Verification
1. **サイドバー表示確認**
   - サイドバーが細くなっていること（テキストが折り返されたり崩れたりしていないか注意）。
   - トグルボタンが左上にぴったり寄っていること。
   - ナビゲーションアイコンがボタンのすぐ下から始まっていること。
2. **作業領域確認**
   - フォルダツリー等のコンテンツが、サイドバーの変更に合わせて左に寄り、かつ上端ギリギリから表示されていること。
