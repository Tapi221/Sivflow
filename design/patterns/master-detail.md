# Master Detail

- Shared contract: list-item states, pane spacing, panel elevation, and breakpoints expressed as intent.
- React now: explorer/sidebar plus content pane flows.
- SwiftUI later: `NavigationSplitView` adapts the same semantic roles.
- Compose later: adaptive navigation rail / list-detail layouts reuse the same token names.
- Excluded: platform-specific navigation chrome and window management.
