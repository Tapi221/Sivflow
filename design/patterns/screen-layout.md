# Screen Layout

- Shared contract: spacing scale, panel widths, surface tiers, and semantic text roles.
- React now: mixed Tailwind classes and CSS variables.
- SwiftUI later: map tokens to environment-backed spacing and semantic colors.
- Compose later: map tokens to `ColorScheme`, `Typography`, and custom spacing locals.
- Excluded: Electron title bar and desktop window chrome.
