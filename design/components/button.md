# Button

- Purpose: primary action, secondary action, quiet action, destructive action.
- Base tokens: `semantic.color.action.*`, `semantic.color.text.onPrimary`, `semantic.radius.control`, `semantic.motion.interactive`, `semantic.elevation.controlRaised`.
- Current React references: `src/components/ui/button.tsx`, `src/components/ui/surface-button.tsx`.
- Interaction states: default, hover, focus-visible, disabled, destructive.
- Platform note: shared contract defines tokens and state names only; hit slop and platform feedback stay native.
