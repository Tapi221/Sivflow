# Card

- Purpose: grouped content surfaces, dashboards, study summaries, and content panes.
- Base tokens: `semantic.color.background.app`, `semantic.color.border.default`, `semantic.radius.card`, `semantic.radius.panel`, `semantic.elevation.card`, `semantic.elevation.panel`.
- Current React references: `src/components/ui/card.tsx`, `src/features/study/PracticeSummary.tsx`.
- Interaction states: static, hover-lift where already present, selected when feature-owned.
- Platform note: shared contract does not prescribe card composition or navigation rules.
