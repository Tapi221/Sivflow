# Tabs

- Purpose: segmented workspace switching and compact content category switching.
- Base tokens: `semantic.color.text.*`, `semantic.color.background.sidebar`, `semantic.color.background.sidebarActive`, `semantic.radius.pill`, `semantic.motion.interactive`.
- Current React references: `src/components/explorer/ExplorerTabs.tsx`.
- Interaction states: inactive, active, hover, keyboard focus.
- Platform note: tab indicators can be underline, pill, or segmented-control depending on platform conventions.
