# Input

- Purpose: single-line text entry, search, inline metadata editing.
- Base tokens: `semantic.color.background.app`, `semantic.color.border.*`, `semantic.color.text.*`, `semantic.radius.control`, `semantic.elevation.controlInset`, `semantic.motion.interactive`.
- Current React references: `src/components/ui/input.tsx`, `src/components/card/editor/LinkEditor.tsx`.
- Interaction states: default, placeholder, focus, disabled, invalid.
- Platform note: text selection handles, IME behavior, and autofill remain platform-specific.
